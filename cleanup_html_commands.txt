# Remove the old index.html from the root directory
git rm /home/dsl/gemini/chrust_plus/index.html

# Remove the old index.html from the gemini_upgrade directory
git rm /home/dsl/gemini/chrust_plus/gemini_upgrade/index.html

# Commit the removal of these files
git commit -m "Remove old/unused index.html files"

# Push the changes to your main branch
git push origin main
